import { connect } from 'react-redux';
import FlowsWidgetComponent from './FlowsWidgetComponent.jsx';

const mapStateToProps = ({

}) => ({

});

const mapDispatchToProps = ({

}) => ({

});

export default connect(null, null)(FlowsWidgetComponent);
