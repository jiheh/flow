import { connect } from 'react-redux';
import MeditationWidgetComponent from './MeditationWidgetComponent.jsx';

const mapStateToProps = ({

}) => ({

});

const mapDispatchToProps = ({

}) => ({

});

export default connect(null, null)(MeditationWidgetComponent);
