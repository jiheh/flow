import React, { Component, PropTypes } from 'react';

import ReflectionWidget from './ReflectionWidget.jsx'; // import dumb component

class ReflectionWidgetComponent extends Component {


  render() {
    const {  } = this.props;

    return (
      <ReflectionWidget

      />
    );
  }
}

export default ReflectionWidgetComponent;
