import { connect } from 'react-redux';
import ReflectionWidgetComponent from './ReflectionWidgetComponent.jsx';

const mapStateToProps = ({

}) => ({

});

const mapDispatchToProps = ({

}) => ({

});

export default connect(null, null)(ReflectionWidgetComponent);
